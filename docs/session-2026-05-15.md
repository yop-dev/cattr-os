# Session Findings — 2026-05-15

Summary of all work completed in this session.

---

## What Was Done

### C-022 — Hide In-Progress Intervals

**Status:** ✅ Done and confirmed working.

**Problem:** The desktop agent pushes periodic 3-minute intervals to the server during an active tracking session. These appear immediately in the DB and show up in Reports, Screenshots, and the Dashboard sidebar while the timer is still running — confusing, since the session isn't finished yet.

**Solution:** Client-side filter using `POST /api/tracking/current`. After fetching intervals (or reading from the Vuex store), call the endpoint to get the active session. Strip any intervals belonging to it before rendering.

**Filter logic:** An interval is excluded when ALL of: (1) belongs to the current user, (2) task matches the active session's `task_id`, (3) `start_at >= session.start_at`. If no session is active, all intervals show normally.

#### Files Modified

| File | Repo | Change |
|---|---|---|
| `app/public/timecard-export.js` | cattr-server | Added `filterActiveSession()`; modified `renderTimecard()` to fetch active session and filter before `buildContent()` |
| `app/public/screenshots-grouped.js` | cattr-server | Added `fetchCurrentSession()` + `filterActiveSession()`; chained into `fetchIntervals()` call site |
| `app/public/quick-create.js` | cattr-server | `showRunningState()`: sets `window.__cattrCurrentSession = session`; `showIdleState()`: sets it to `null` |
| `app/public/dashboard-nav.js` | cattr-server | Added `dnNormTs()`; C-022 filter block in `injectSidebarTimes()` using `window.__cattrCurrentSession` |

**Key technical notes:**
- `timecard-export.js` uses nested `iv.user.id` / `iv.task.id` (API shape); `dashboard-nav.js` originally used flat `iv.task_id` (Vuex store shape)
- `quick-create.js` already polls `tracking/current` every 1s — exposing its result on `window` avoids extra API calls in the dashboard
- Added `|| !activeSession.start_at` null-guard to `filterActiveSession()` to handle edge case of session object with missing timestamp

---

### Dashboard Sidebar — Per-Interval Rows (matching Reports format)

**Status:** ✅ Done.

**Problem:** After C-022 landed, the Dashboard sidebar showed an aggregated time range (e.g., "12:56 AM – 1:33 AM") across all intervals for the day. This was misleading — it implied continuous work when there were gaps. User wanted it to match the Reports page format: each session shown as its own row with bold duration and gray time range below.

**Solution (iterated):**

1. **Removed the aggregated range** — replaced `taskRanges` (start/end tracking) with per-interval rows.
2. **Added `fmtTime()` / `fmtDur()` helpers** for formatting timestamps and durations.
3. **CSS**: Added `flex-wrap: wrap` to `li.task`; added `.dn-iv-list`, `.dn-iv-row`, `.dn-iv-dur`, `.dn-iv-range` styles; changed `.dn-iv-row` to `flex-direction: column` so duration stacks above time range (matching Reports layout).
4. **Merge contiguous intervals**: Initially tried merging from the Vuex store data with a 30s threshold — failed because Vuex timestamps have slightly lower precision than API responses (32s gap wasn't merging). Bumping to 60s over-merged (collapsed separate sessions).
5. **Switched to API data source**: Replaced Vuex store reading with a direct call to `POST /api/time-intervals/list` using the exact same QueryHelper format as `timecard-export.js` (with `with`, `where`, `orderBy`, `perPage`). Applied the exact same 30s merge. Results now always match Reports.
6. **Cache with session-change invalidation**: `_dnSidebarCache` (15s TTL); `_dnLastSessKey` tracks session start/stop — clears cache on change so stopped session's intervals appear immediately.
7. **Immediate injection after fetch**: Without the timebar's D3 DOM mutations, the MutationObserver no longer fires reliably after an async fetch. Fixed by calling `injectSidebarTimes()` directly from the fetch callback instead of waiting for the observer.

#### Files Modified

| File | Repo | Change |
|---|---|---|
| `app/public/dashboard-nav.js` | cattr-server | Per-interval rows; `fetchSidebarIntervals()`; API-based data source; `fmtTime()`/`fmtDur()`; cache + session invalidation; immediate post-fetch injection |

---

### BUG-027 — Dashboard Timebar Removed

**Status:** ✅ Done.

**Decision:** The timeline bar (D3 visualization, `.at-container.intervals`) was causing more problems than value — it required a mousedown capture hack to suppress click popups, and the sidebar interval rows now show the same information in a cleaner format.

**Fix:**
- Changed `display: none !important` on `.at-container.intervals` in CSS
- Removed `patchTimelineClick()` function and its `tick()` call entirely

#### Files Modified

| File | Repo | Change |
|---|---|---|
| `app/public/dashboard-nav.js` | cattr-server | CSS: hide `.at-container.intervals`; removed `patchTimelineClick()` |

---

### BUG-025 — Desktop Duplicate Intervals Fix

**Status:** ✅ Fixed and DB-verified.

**Problem:** A ~1–2 minute desktop session intermittently produced 3 DB intervals instead of 1: two with identical `start_at` + one short tail. Root cause: `stop()` sets `this.active = false` only **after** `await captureCurrentInterval()` completes (50–500ms async window). Any concurrent caller reaching `captureCurrentInterval()` in that window passed the `!this.active` guard and pushed a duplicate. A display patch (`mergeContiguousIntervals`) was hiding this in Reports but dirty data continued accumulating.

**Fix:** Three concurrency guards added to `app/src/base/task-tracker.js` in `desktop-application`:

1. **`_stopInProgress` mutex on `stop()`** — blocks concurrent re-entry; returns `false` immediately if already stopping; `finally` guarantees flag reset even if `captureCurrentInterval()` throws
2. **Return-value check in `start()`'s task-switch path** — throws `UIError(409)` if the internal `stop(true, false)` call is blocked, preventing silent state corruption where the original `stop()`'s cleanup would overwrite the new session's state
3. **`_captureInProgress` mutex on `captureCurrentInterval()`** — blocks concurrent captures from any path including the floating (unawaited) `interval-capture` event handler; `finally` guarantees flag reset

**Verified:** 3 test sessions (32s, 91s, task-switch) — DB shows exactly 2 rows, no duplicates, no tail.

#### Files Modified

| File | Repo | Change |
|---|---|---|
| `app/src/base/task-tracker.js` | desktop-application | `_stopInProgress` + `_captureInProgress` flags; guards on `stop()`, `captureCurrentInterval()`, `start()` task-switch path |
| `docs/superpowers/specs/2026-05-15-bug-025-duplicate-intervals-fix-design.md` | cattr-server | Design spec |
| `docs/superpowers/plans/2026-05-15-bug-025-duplicate-intervals-fix.md` | cattr-server | Implementation plan |

---

## Edge Cases Identified

### EC-001 — "Too Many Attempts" errors / occasional logout

**Status:** ✅ Fixed — 2026-05-15 (session continuation)

Per-user rate limiter added to `app/routes/api.php`. See BUG section in `cattr-tracker.md`.

---

## Session Continuation — 2026-05-15 (same day, later)

### BUG-028 — Team page timeline bars showing at UTC instead of local time

**Status:** ✅ Fixed.

**Root cause:** `DashboardExport::collection()` computed `from_midnight` using `Carbon::make($interval->start_at)` (UTC) then called `startOfDay()` which returned UTC midnight. Sessions at e.g. 12:56 AM PDT (= 7:56 AM UTC) got `from_midnight = 28560`, appearing at the 8 AM mark on the Team page bar.

**Fix:** `$start = Carbon::make($interval->start_at)?->setTimezone($tz)` where `$tz = $that->userTimezone ?? $that->companyTimezone ?? 'UTC'`. `startOfDay()` then returns local midnight. `duration` (absolute diff) is unaffected.

**Files:** `app/app/Reports/DashboardExport.php` (new file in repo) + `Dockerfile` COPY line added.

---

### C-024 — Remove Team nav link; rename Reports → "Team Reports" for admins

**Status:** ✅ Done.

- Removed `injectTeamLink()` and all Team-link CSS from `dashboard-nav.js`
- `flattenReportsDropdown()` now shows "Team Reports" for admin/manager/auditor users and "Reports" for employees
- Guarded text update with `!==` check to prevent MutationObserver infinite loop (textContent write → DOM mutation → observer fires → textContent write → …)

**Files:** `app/public/dashboard-nav.js`

---

### EC-001 — Per-user rate limiting on tracking routes

**Status:** ✅ Fixed.

**Root cause:** `throttle:600,1` is keyed by IP. With 10 users at 1 req/s = 600 req/min, the shared bucket fills exactly at the limit, causing 429 errors for all users simultaneously.

**Fix:** Named `RateLimiter::for('tracking-per-user', ...)` keyed by `$request->user()->id`. Each user gets their own 600 req/min bucket.

**Files:** `app/routes/api.php`

---

### MutationObserver infinite loop fix

**Status:** ✅ Fixed.

`flattenReportsDropdown()` was setting `a.textContent = label` unconditionally on every MutationObserver tick. Writing to `textContent` is itself a DOM mutation, triggering the observer again → infinite loop pegging the browser CPU. Fixed with a `!==` guard.

---

## Files Changed (full session)

| File | Repo | Change |
|---|---|---|
| `app/public/timecard-export.js` | cattr-server | C-022: `filterActiveSession()` + modified `renderTimecard()` |
| `app/public/screenshots-grouped.js` | cattr-server | C-022: `fetchCurrentSession()` + `filterActiveSession()` + chained call site |
| `app/public/quick-create.js` | cattr-server | C-022: expose `window.__cattrCurrentSession` in `showRunningState()` / `showIdleState()` |
| `app/public/dashboard-nav.js` | cattr-server | C-022 filter; per-interval sidebar rows; API data source; timebar removal; C-024 nav changes; infinite loop fix |
| `app/app/Reports/DashboardExport.php` | cattr-server | BUG-028: apply userTimezone to from_midnight calculation |
| `app/routes/api.php` | cattr-server | EC-001: per-user rate limiter for tracking routes |
| `Dockerfile` | cattr-server | BUG-028: COPY override for DashboardExport.php |
| `docs/cattr-tracker.md` | cattr-server | BUG-025, BUG-027, BUG-028, EC-001 marked fixed; C-022, C-023, C-024 added |
| `app/src/base/task-tracker.js` | desktop-application | BUG-025: three concurrency guards |

---

## Confirmed Working This Session

| ID | Result |
|---|---|
| C-022 | ✅ In-progress intervals hidden from Reports, Screenshots, and Dashboard sidebar |
| C-023 | ✅ Dashboard sidebar per-interval rows match Reports format |
| C-024 | ✅ Team nav link removed; Reports renamed to "Team Reports" for admins |
| BUG-025 | ✅ No duplicate intervals |
| BUG-027 | ✅ Timebar removed |
| BUG-028 | ✅ Team page bars show at correct local time |
| EC-001 | ✅ Per-user rate limiting — no more shared bucket exhaustion |

---

---

## Session Continuation — 2026-05-15 (EC safe-fixes batch)

### EC Safe Fixes — 8 items fixed

All "safe" (no breaking-change risk) items from `docs/edge-cases-tracker.md` were implemented and confirmed working.

| ID | Fix | Files |
|---|---|---|
| EC-005 | Deferred queue ORDER BY startAt ASC | `app/src/base/deferred-handler.js` |
| EC-007 | `.catch()` on `onTaskCreated` startTrack dispatch | `app/renderer/js/components/user/ControlBar.vue` |
| EC-009 | `saveEdit()` nested try/catch with first-PATCH revert on second-call failure | `app/public/timecard-export.js` |
| EC-010 | Edit modal timezone label uses `Intl.DateTimeFormat().resolvedOptions().timeZone` | `app/public/timecard-export.js` |
| EC-012 | `_trackingInFlight` in-flight guard on `startTaskFromCard()` | `app/public/dashboard-nav.js` |
| EC-016 | `if (this.loading) return;` guard on `submit()` | `app/renderer/js/components/user/tasks/TaskCreateModal.vue` |
| EC-018 | `doExportPDF()` made async; button disabled + 50ms yield before export | `app/public/timecard-export.js` |
| EC-019 | `_clockSkewWarned` flag + HTTP `Date` header check in `pollOnce()`; emits `clock-skew-warning` IPC event | `app/src/base/web-sync.js` |

Added as BUG-029 through BUG-036 in `cattr-tracker.md`.

**Tested and confirmed working:** EC-010 (timezone label visible), EC-018 (loading state on export), EC-012 (double-click blocked in network tab), EC-016 (single task created on double-click).

**Deployed:** Local Docker rebuilt and running. VPS not yet updated — push when ready.

---

## Pending at End of Session

| ID | Title | Priority | Next Step |
|---|---|---|---|
| EC-003 | No database backup on VPS | High | ⏸️ On hold — needs purchasing authority for DO backups |
| EC-008 | Token expiry mid-session — silent data loss | High | Investigate `authentication.js` token refresh; scope 401 detection carefully |
| EC-011 | 422 handler too broad in deferred-handler | Medium | Verify Laravel error message format before implementing |
| EC-013 | DOM-driven session state in dashboard-nav.js | Medium | Cache `tracking/current` result; high regression risk — test carefully |
| EC-017 | Merged intervals spanning midnight show wrong date | Medium | Add date-boundary check to `mergeContiguousIntervals()`; verify C-022 filter still works |
| EC-004 | Laptop sleep creates phantom intervals | Low | Add `powerMonitor` suspend hook; test on real hardware |
| EC-014 | Deferred queue bleeds across users on shared machine | Low | Filter `findAll` by current user ID |
| EC-015 | Desktop offline when web stops session | Low | Investigate `web-sync.js` reconnect behavior first |
